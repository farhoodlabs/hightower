apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hightower-workspaces
  namespace: shannon
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 10Gi
